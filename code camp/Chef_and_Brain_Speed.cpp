#include <bits/stdc++.h>

using namespace std;
#define ll long long

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int x,y;
    cin >> x >> y;
    if(y>x){
        cout<<"YES"<<endl;
    }
    else{
        cout<<"NO"<<endl;
    }
}